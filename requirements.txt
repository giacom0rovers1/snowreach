# Tested with the versions on the right, which is what produced the numbers in
# the README. Anything reasonably recent should work: nothing here uses an
# interface that has changed lately.
numpy>=1.24
pandas>=2.0
xarray>=2023.1
netcdf4>=1.6
