# The root .gitignore excludes every .nc. The worked example is the one
# exception: it has to be a tracked file, because Zenodo archives this
# folder as it stands at a release and would otherwise publish a package
# whose example is missing.
!example/*.nc
__pycache__/
*.pyc
snowreach-*.zip
